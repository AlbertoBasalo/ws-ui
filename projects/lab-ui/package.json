{
  "name": "@lab/ui",
  "description": "A library with Angular UI components, pipes, and directives for the Lab project.",
  "version": "0.0.4",
  "scripts": {
    "ng": "ng",
    "prerelease": "standard-version ",
    "release": "git push --follow-tags origin main",
    "prepublish": "ng build --configuration production && npm run release",
    "publish": "cd ../../dist/lab-ui && npm link"
  },
  "peerDependencies": {
    "@angular/common": "^18.0.0",
    "@angular/core": "^18.0.0"
  },
  "dependencies": {
    "tslib": "^2.3.0"
  },
  "sideEffects": false
}
